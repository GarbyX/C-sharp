using System;

namespace MyApplication
{
  class Program
  {
    static int PlusMethod1(int a, int b)
    {
      return a + b;
    }

    static double PlusMethod2(double a, double b)
    {
      return a + b;
    }

    static void Main(string[] args)
    {
      int myNum1 = PlusMethodInt(10, 15);
      double myNum2 = PlusMethodDouble(7.6, 9.45);
      Console.WriteLine("Int: " + myNum1);
      Console.WriteLine("Double: " + myNum2);
    }  
  }
}

/*
Outputs:
prog.cs(19,20): error CS0103: The name `PlusMethodInt' does not exist in the current context
prog.cs(20,23): error CS0103: The name `PlusMethodDouble' does not exist in the current context
Compilation failed: 2 error(s), 0 warnings
*/
